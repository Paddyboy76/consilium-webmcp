# Adversarial review

## A — Hackathon judge

| Criterion | Score | Evidence / highest-leverage improvement |
|---|---:|---|
| WebMCP leverage | 9.2 | Typed council workflow, visible shared state, dynamic commit lifecycle. Validate in ChatGPT browser. |
| Execution | 8.2 | Complete deterministic flow and tests. Activate genuine Agents SDK runtime and live HTTPS deployment. |
| Potential impact | 9.0 | Persistent user-owned memory makes external agents meaningfully context-aware. |
| Creativity / ambition | 9.1 | A chosen external agent collaborating with an internal evidence council is distinct from dashboard automation. |

## B — Skeptical engineer

- **“The agents are fake.”** Fair for the credential-free runtime: it labels itself `deterministic_demo` and makes no live-model claim. Remaining P0 is the Agents SDK adapter with genuine agents-as-tools.
- **“Citations are cosmetic.”** Reports cite retrieved IDs; invented IDs deterministically force abstention; tests prove it.
- **“WebMCP adds nothing.”** The external agent gets a stable semantic boundary, shared visible state, and capability lifecycle unavailable from UI guessing.
- **“Actions bypass control.”** Consult is read-only; proposal does not persist; commit is session-scoped, one-use, and dynamically removed.
- **“The demo is staged.”** Reset is deterministic by design, but all tool calls execute real application paths and trace IDs can be inspected.

## C — First-time user

The thesis, 45-minute constraint, memory pattern, three specialists, trace, and human-control panel are visible without architecture knowledge. The main remaining comprehension risk is browser WebMCP availability; the status pill explicitly distinguishes native support from UI-only demo mode.

